FROM node:14.15.1-alpine

ARG WORKDIR
ARG API_URL
# ARG CONTAINER_PORT

ENV HOME=/${WORKDIR} \
    LANG=C.UTF-8 \
    TZ=Asia/Tokyo \
    HOST=0.0.0.0 \
    API_URL=${API_URL}

# RUN echo ${HOME}
# RUN echo ${CONTAINER_PORT}

WORKDIR ${HOME}

# EXPOSE ${CONTAINER_PORT}